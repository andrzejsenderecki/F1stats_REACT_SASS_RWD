import React from 'react';
import '../App.scss';

const Loading = () => {
    return (
        <div className='row loadingPosition'>
            <div className='loading'/>
        </div>
    )
}

export default Loading;